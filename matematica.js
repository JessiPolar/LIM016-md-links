const sumar = (a,b) => {
    return a + b
}

const restar = (a,b) => {
    return a - b
}

const multiplicar = (a,b) => {
    return a * b
}

const dividir = (a,b) => {
    return a / b
}

const name = "hello world"

//module.exports = {name}

module.exports = {name, sumar,restar,multiplicar,dividir}